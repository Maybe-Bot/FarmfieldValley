You are helping continue development on Farmfield Valley, a local-first-for-now farm project management and annual crop planning web app prototype.

The user is building this as a practical vegetable-farm planning tool. They are not a professional software engineer, so communicate clearly, avoid over-engineering, and explain important setup or data-risk issues plainly. The user values visual/map-based workflows and wants the app to eventually become a serious farm planning and work-recording system.

Current project location:
<project-root>

Current stack:
- Frontend: React + TypeScript + Vite
- Map/UI: Leaflet
- Backend: Node + TypeScript + Express
- Database: PostgreSQL + PostGIS
- Local single-machine development right now
- Multi-user accounts have been added, but this is still beta/prototype quality
- Optional offline imagery support exists again through a local tile cache served by the API. It is useful but not the current product priority unless the user asks about maps/offline work.
- This folder is now a local Git repository on branch main. Initial baseline commit: e4dc8b4 "Initial Farmfield Valley project state". The repo has a local placeholder identity: Farmfield Valley Local <farmfield-valley-local@example.invalid>. Change this before pushing to a remote.

Run commands:
- Install: npm install
- Start database: docker compose up -d
- Run migrations: npm run db:migrate
- Start app: npm run dev
- Frontend: http://localhost:5173
- API: http://localhost:4000
- Tests: npm test
- Production build check: npm run build

Important warning:
- Do not run db:seed against real user data unless the user explicitly asks and understands it can wipe demo/prototype tables. The seed script now has a destructive-confirmation guard, but still treat it as destructive.
- Running npm run db:migrate is the right action after new migrations. Do not run seed just because a migration was added.

Core product concept:
Plan the season -> record what actually happened -> automatically shift future task timing when actual dates differ from planned dates.

Primary mental model:
- Farm -> Field -> Block -> Bed
- Crop plan records are parent plantings.
- Actual bed usage can be split into placements.
- The map is central. The user wants to build plans visually on the map and record work by clicking map objects.

Long-term product direction:
- The app should represent farm land over time, not only current state.
- Field/block/bed/crop/cover-crop states should be assessable by date using a map time slider.
- Future versions should support area events and point events over time, possibly with high-detail plant placement.
- It should be able to hold detailed plant-level data only when needed, without bloating the data model for normal bed/block-level planning.
- It may eventually export/render data into game engines or 3D environments for robotics.
- It may eventually use photos/drone imagery to refine bed/plant placement.
- It may eventually include seed/genetics catalogs from seed companies, starting with Johnny's and High Mowing, but that is not a current priority.

Major design decisions already made:
- Use an event/timeline-capable model for field state instead of overwriting current state only.
- Preserve planned dates and actual dates.
- Use milestone/flow-chart-based task scheduling instead of fixed-only task dates.
- Keep beds mostly map geometry, not individual plant coordinates.
- Represent exact plant counts as lengths/sections of beds rather than visible individual plant dots by default.
- Show more geometry detail only at closer zooms.
- Roads/paths can be represented in the bed layer but should not be plantable.
- Workers can record task completion; planners/owners can adjust plans.
- Most users should not need to understand the database model; workflows should be visual and obvious.

Accounts and permissions:
- There are farm accounts with username/password.
- There are at least two practical privilege levels:
  - Planner/owner: can plan and adjust farm data.
  - Worker/employee: can record task completion.
- Admin control panel exists or is being built for global/admin functions.
- Feedback/suggestion/problem reporting should be available to all users, but reports should only be visible in admin.
- Maps are visible to all users unless private mode is turned on in settings.

Map system:
- Uses Leaflet with aerial imagery.
- Fields and blocks are polygons.
- Beds are generated within blocks and have geometry stored in PostGIS.
- Blocks and beds can be selected on the map.
- The user wants the map to be mouse-first and practical, not fancy.
- The map has planning and working modes. Planning mode is for layout/crop plan changes. Working mode is for recording actual work from the map.
- The map time control should default to the current week, with week back/forward buttons and a time slider still available.
- Crop labels should only show crops when they are actually in the ground according to the selected map time.
- Crop labels should be short: crop + variety, not status clutter.
- Old/past-year data should not clutter current allocation/task workflows unless the user intentionally looks back in time.

Map/bed-generator notes and possible issues:
- The bed generator previously sometimes cut generated beds short when zoomed into part of a field. Verify whether this still reproduces before assuming it is current. The intended behavior is to generate full-length beds based on the selected block geometry, not the current viewport.
- The first-bed offset has been unstable. The backend uses one-sided PostGIS ST_Buffer with side=left/right, so the picked line should generally be treated as a bed edge, not a centerline.
- If beds already exist in a block, adding a new bed should normally build off the selected bed, not start again from the block edge, unless the user selects “replace existing beds.” Selected-bed extension exists; block-only behavior may still need UX review.
- The direction arrow was reported as confusing. Verify current UI before changing it.
- Bed deletion has a frontend call and backend route. If deletion fails, first check whether the bed has planned plantings, placements, or harvest records; those intentionally block deletion.
- Road/path presets were added using bed_presets.is_road and beds.source = 'road'. Roads should show on the map but not be available for planting, placement, or harvest logging.
- The bed generator UI should prefer a simple path:
  - Select block or bed.
  - Choose preset.
  - Make one bed or fill block.
  - Advanced line/curved controls are hidden lower down.
- User wants harvest/access roads. Their farm often skips two bed slots for roads, but other farms may use other systems. A road preset all path/no bed is acceptable and should not be plantable.
- Beds need to store exact geometry in a way that can later be updated from drone/photo/survey data.

Current desired bed workflow:
- If no beds exist or user chooses replace all, build from a selected block edge inward.
- If a bed is selected, “Make one bed” should build the next bed off that selected bed by default.
- After making one bed, select the new bed automatically so repeated clicks keep extending the bed set.
- If filling a block, generate as many beds as fit from the chosen starting edge/bed, respecting preset bed width and path spacing.
- Generated beds should square/clip cleanly at irregular block edges; avoid triangle spikes sticking out.
- If beds planned vs actual differ, the app should eventually support recording actual bed count and squish/stretch path spacing to fit reality.
- Making beds in planning should create a bed-making task for that block, even if no due date is known yet. Once crop dates in the block exist, schedule the task before beds are needed.

Crop planning:
- Annual Crop Plan should be grouped by year and hide older years by default.
- “Parent Planting” was developer terminology and is confusing to the user. In UI, avoid “parent/child placement” language where possible.
- Current user-facing planting form should read “Plan Planting.”
- Use “Planting date,” not “watered in date.”
- Crop and variety dropdowns should be prominent.
- Planting records now store intended_field_id, intended_block_id, and intended_bed_id separately. This fixed a bug where choosing a block while editing a planting did not save unless an exact bed was selected.
- Planting records now also store spreadsheet/import agronomy details: days_to_harvest, field_spacing_in_row, row_spacing, rows_per_bed, cells_per_tray, dead_at_frost, and bed_cover.
- Rows per bed is especially important to the user because it affects plant-count/bed-length thinking. Keep it visible and editable in planting records.
- Lot number should be a free text field around 128 characters.
- Seed/genetics structure desired:
  - Higher categories can be hidden or auto-filled.
  - User-facing fields should include crop, variety, breed, supplier, lot number, possibly catalog number.
  - Levels should be skippable.
  - Example hierarchy: Nightshade -> pepper -> jalapeno -> variety/breed -> supplier -> lot number.
- For transplants:
  - Include tray count calculator.
  - Standard tray sizes: 128, 288, 72, 50.
  - Tray location should be optional and eventually map-based.
- For direct seeding:
  - Use seed spacing x bed length.
- Intended bed/location should use dropdowns: field/block first, then bed.
- Spacing should be two numeric boxes in inch or cm depending on metric/ft setting.

Bed allocation from map:
- User wants to select a bed on the map, choose a likely planting from a dropdown, and allocate plants visually.
- Dropdown should prioritize:
  - right field + right date first
  - right date + nearby fields next
- Slider should adjust plant count and bed length/sections.
- Slider should start around 90% of available seeded/transplant count.
- It should subtract already placed plants, but allow fudge room up to roughly 20% because actual germination/transplant counts vary.
- There should be a clear way to say whether the allocation uses all remaining plants or only part.
- Placement should fill beds from one side and wrap to the next bed with no space unless specified.
- At close zoom, show sections of bed by crop. At farther zoom, hide individual beds and show summarized crop/block labels.

Tasks and task flows:
- Task flow chart system exists and was reworked to be more visual.
- The user wants task flows as editable visual flow charts, not tangled lists.
- Nodes can be connected up/downstream.
- Node details should be editable on the node itself via an edit icon/expanded fields.
- Task flow nodes should support icons and two editable colors.
- An 8-bit tractor PNG exists in demo data and has been used or should be used for task icons. The 3/4 tractor view should be primary in forms/task lists. On the map, tractor icons should move along beds needing work that week, using closest directional view and flipping sprites if useful.
- Task icons should appear in task flow, task list, and map.
- Current tasks should focus on current year/week by default. Past years should not bog down current task list.
- Task list has week back/forward buttons and should default to current week.
- Marking tasks done should be easy.
- If a task is scheduled within +/-4 weeks, record date defaults to today. If older, it defaults to planned week/date.
- Marking planting in the future is not allowed.
- Recording from a task should preserve enough information to keep the system useful, especially for planting/transplanting tasks.
- There was a bug where task recalculation deleted/recreated tasks and broke identity/history. This was addressed by updating tasks by flow-node identity rather than destroying them. Preserve task identity going forward.

Record work:
- Workers should be able to record completion.
- Planner can adjust planting details while recording from a task.
- For “plant in field” task recording:
  - Date is near top and defaults appropriately.
  - Notes should be at bottom.
  - Remove confusing “adjust planting from...” wording.
  - Bed placement should autofill from plan but be editable via field/block/bed dropdowns.
  - Spacing should be two numeric boxes in inch/cm.
  - Plant count should calculate bed length based on in-row spacing.
  - Recording should withdraw from available transplants with some fudge room.

Cover crop and field/block state:
- Cover crop is important and should be treated as land/state planning, not normal harvested crop.
- Blocks are not always fully prepped; blocks may be split into beds, cover crop, unassigned/empty parts.
- The user disliked drawing polygons for every cover crop case.
- Desired cover-crop/block-state workflows:
  - Fill whole block with a state/use.
  - Or split a block by a two-point line whose endpoints are on the block edge.
  - The unused split-off part should be “unassigned,” not “reserve.”
- Blocks should have current state assigned when made, and may separately have future use assignments.
- Current state needs time/date history.
- Cover crop needs planned dates and actual work dates.
- Readiness/cleanup states should include needs mowing, plastic beds need removing, and notes for specific cleanup like tomato stakes.

Spreadsheet import:
- The first spreadsheet work was built around messy demo data. The user now wants a stricter simpler user-accessible uploader.
- The uploader is strict about the header row, but rows with missing essential data should still import as planting records and be flagged for review instead of aborting the whole upload.
- The upload card now has a "Download CSV template" button with a correctly formatted example row.
- Current exact columns:
  - Seed supplier
  - Crop
  - Variety
  - Catalog number
  - Start date
  - Plant count
  - Transplant date
  - Tray count
  - Cells per tray
  - Days to harvest
  - Field spacing in row
  - Row spacing
  - Rows per bed
  - Dead at frost (y/n)
  - Bed cover (plastic/bare)
  - Field
  - Block
  - Bed
  - Notes may be useful
- The importer accepts blank/plastic/bare for Bed cover.
- Missing essentials currently include Crop, Start date, Plant count, Field, and Block. Those rows import, use harmless placeholders where required by the database, and add a "Needs manual completion:" line to notes.
- Missing optional/review data adds an "Optional fields to review:" line to notes. The crop plan list and planting detail/edit screens show a small rounded square by the crop/field labels: red for major issues and yellow for optional missing details.
- New imports should not put "Needs completion" in the planting title. Older imported rows may still have that prefix; the UI strips it visually where practical.
- Spreadsheet-imported field/block/bed values are now stored in real planting location fields where they match existing map objects.
- Fertility plans and records should NOT be imported yet. That is a future v0.0.2-style feature.

Feedback/reporting:
- User wants a button labeled “suggestion/problem,” not “report problem.”
- It should be available to all users, including login page.
- Submission should save recent user actions/context plus optional comment in a way the admin can review.
- Reports should only be visible in admin/control panel.

Undo/redo:
- Undo/redo exists for a few steps within a session.
- Important safety fix: undo/redo must only affect changes made within the current account/user context. It previously risked farm-wide restore that could erase another user’s work.
- Do not expand undo beyond practical local/session safety without careful design.

Settings:
- Includes metric/ft switch.
- ft mode should convert bed spacing, bed length, plant spacing, and area labels where visible.
- Area should display in acres unless under 0.125 acres, then sq ft or sq m depending on unit setting.
- Includes dark view option.
- Includes private maps toggle.

Hosting/security:
- The app was audited for free hosting. For a small beta, frontend/backend/database/maps/auth all need consideration.
- API base URL uses Vite env var VITE_API_BASE_URL with localhost fallback for local dev.
- Production build should pass.
- Public internet requires stricter CORS, secure cookies, CSRF secret, and better auth configuration.
- If NODE_ENV=production, CSRF_SECRET is required.
- User has had trouble editing .env by trying to execute it. Remind them .env is a text file to open/edit, not run.
- Before pushing to GitHub/GitLab or connecting deployment, review demo credentials, .env handling, and anything that should not be public. Prefer a private remote first.

Known local setup pain points:
- Docker permission issues can happen if user is not in docker group.
- PostgreSQL connection errors often show ECONNREFUSED 127.0.0.1:5432 when Docker/Postgres is not running.
- npm may not be installed on fresh machine.
- curl may not be installed.
- Browser refresh alone does not restart backend/frontend dev server.
- If app changes do not appear, stop npm run dev with Ctrl+C and run npm run dev again.

Demo data:
- There is a “demo data” folder with spreadsheets and an 8-bit tractor PNG.
- User created demo accounts during development; do not assume current credentials are safe to share.
- User has real-ish drawn fields/blocks they do not want deleted. Never run seed/import code that truncates data unless explicitly approved.
- When using demo spreadsheet data, user wanted only certain fields/blocks such as B1-3 and H2-6, and wanted existing drawn fields/blocks preserved.
- .gitignore keeps local demo spreadsheet plans (*.xlsx/*.ods), .env, node_modules, build output, and offline imagery cache out of Git. The tractor PNG asset is tracked.

Coding guidance for the next assistant:
- Inspect code before editing.
- Do not assume user data can be reset.
- Do not run db:seed unless explicitly asked.
- Prefer small practical changes over rewrites unless the user asks for architecture work.
- Add comments for non-obvious logic. The user requested explanatory comments for future maintainers.
- Use tests/build after changes:
  - npm test
  - npm run build
- If migrations are added, run npm run db:migrate, not db:seed.
- Keep UI farmer-friendly: clear buttons, simple language, direct map workflows.
- Avoid exposing developer terms like “child placement” in user-facing UI unless necessary.
- If a feature has complex future implications, implement the simple version but leave code structured for extension.

Recent work before this handoff:
- Initialized a local Git repository on branch main and committed baseline e4dc8b4.
- Tightened .gitignore so real .env files, node_modules, build output, offline imagery cache, and local demo spreadsheets do not get committed.
- Added spreadsheet column Bed cover (plastic/bare), plus a downloadable CSV template with an example row.
- Changed spreadsheet import so incomplete rows still import and are flagged for manual review instead of aborting the upload.
- Added red/yellow rounded-square review markers in the crop plan list and planting edit form: red for major missing data, yellow for optional missing details.
- Added migration 025_planting_location_and_import_details.sql. It adds intended_field_id, intended_block_id, and import/agronomy fields to plantings.
- Fixed planting edit bug where selecting a block did not persist unless a bed was also selected. Plantings now store intended field/block/bed separately.
- Added planting record fields/display/editing for days to harvest, field spacing in row, row spacing, rows per bed, cells per tray, dead at frost, and bed cover.
- Ran npm run db:migrate after migration 025; it completed successfully.
- Verification after the latest changes: npm test passed and npm run build passed.

Current known/stale-risk items:
- Bed deletion now has a frontend call and backend route. If it appears not to work, first check whether deletion is intentionally blocked because the bed has planned plantings, placements, or harvest records.
- Selected-bed generation exists and should auto-select the new bed after "Make one bed." Verify in the live UI if the user reports otherwise.
- Block-only bed generation may still need UX refinement around whether it should automatically build from the last existing bed instead of an offset from the picked block edge.
- The old reports about beds being cut short when zoomed into part of a field and the confusing direction arrow may or may not still reproduce; verify before assuming they are current.
- planning.txt has an unrelated uncommitted user edit: "Direct seed SS". Do not discard it.

Suggested next debugging steps if bed generator comes up again:
1. Reproduce in the browser with the user's exact block/zoom/selection path.
2. Confirm the generate-beds request sends full picked line coordinates and not viewport-filtered geometry.
3. Review bed generator state:
   - bedLineSource
   - selectedBedGuideLine
   - layoutStartIndex
   - existingBeds
   - replaceExisting
   - fillWholeBlock
4. If user selects a bed and not replace existing, guide line should come from selected bed edge and layoutStartIndex should create the next adjacent bed/path.
5. If replacing existing or no beds, use picked block edge.
6. Keep PostGIS ST_Buffer side=left/right behavior in mind: the picked line is treated as a bed edge, not a centerline.
